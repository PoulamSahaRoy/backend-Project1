<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-800 text-white p-10">
        <div class="w-full flex items-end justify-end">
            <a class="bg-red-700 px-3 py-2 inline-block mb-4 rounded-md shadow-orange-900 shadow-xl " href="/logout">Log Out</a>
        </div>
        <div class="flex items-start gap-4">
            <div class="w-30 py-2 px-3 text-center h-10 bg-green-600 rounded-md shadow-lg shadow-zinc-900"><a href="/upload">Upload Image</a></div>
            <h3 class="text-3xl mb-5"><span class="text-zinc-400 font-light">Hello,</span> <%=user.name%> </h3>
            <div class="h-12 w-12 rounded-full bg-zinc-600 overflow-hidden shadow-xl shadow-zinc-900">
                <img class="h-full w-full object-cover" src="/images/uploads/<%=user.profileImage%>" alt="">
            </div>
        </div>
        
        <h5 class="text-zinc-400 mb-2">you can create a post.</h5>
        <form action="/post" method="post">
           <textarea class="resize-none bg-zinc-600 border-2 border-zinc-500 rounded-md w-1/3 text-zinc-300 outline-none block shadow-lg shadow-zinc-900 mb-8" name="content" placeholder="What is in your mind" ></textarea>
           <input class="bg-orange-600 shadow-lg shadow-zinc-900 rounded-md text-white py-2 px-3" type="submit" value="Create Post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts.</h3>
            <div class="postcontainer mt-5">
   
                <% user.posts.reverse().forEach((post)=>{ %>
                    <div class="post w-1/3 p-5 border-2 border-zinc-500 rounded-md mt-10">
                        <h4 class=" text-xl text-blue-500 mb-3 font-light">@<%=user.username%></h4>
                        <p class="text-zinc-400 tracking-tight"><%=post.content%></p>
                        <small class="mt-5 inline-block"><%=post.likes.length%> likes</small>
                        <div class="btns flex gap-4 mt-5">
                            <a href="/like/<%=post._id%>" class="text-blue-500"><%=post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike"%></a>
                            <a href="/edit/<%=post._id%>" class="text-zinc-500">Edit</a>
                        </div>
                    </div>
                <% }) %>
                    
            
                
            </div>
        </div>
    </div>
</body>
</html>